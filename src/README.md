The web version of Stellar Elements. Import elements from `@stellar/elements`:

```js static
import {
  Button,
  Checkbox,
  GlobalStyle,
  Input,
  Select,
  Textarea,
} from "@stellar/elements";
```
