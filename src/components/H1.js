import styled from "styled-components";
import { PALETTE } from "../shared";

const H1 = styled.h1`
  font-size: 2rem;
  line-height: 1.25;
  font-weight: normal;
  color: ${PALETTE.black};
  margin: 0;
`;

/** @component */
export default H1;
