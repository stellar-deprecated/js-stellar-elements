import styled from "styled-components";
import { PALETTE } from "../shared";

const H2 = styled.h2`
  font-size: 1.5rem;
  line-height: 1.5;
  font-weight: normal;
  color: ${PALETTE.black};
  margin: 0;
`;

/** @component */
export default H2;
