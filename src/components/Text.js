import styled from "styled-components";
import { PALETTE } from "../shared";

const Text = styled.p`
  font-size: 1rem;
  line-height: 1.75;
  font-weight: normal;
  color: ${PALETTE.black60};
  margin: 0;
`;

/** @component */
export default Text;
